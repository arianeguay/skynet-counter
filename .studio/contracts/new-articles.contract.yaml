name: new-articles
version: 1
schema:
  required_fields:
    - new_count
    - articles
  fields:
    new_count:
      type: integer
    articles:
      type: array
      items:
        type: object
        required_fields:
          - title
          - url
          - source
          - publishedAt
